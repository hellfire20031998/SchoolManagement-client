# Vite: only VITE_* is exposed to the browser.
#
# Local dev with API on your machine: leave empty (uses /api + Vite proxy).
# Vercel / production: set the same in Project → Settings → Environment Variables:
VITE_API_BASE_URL=https://schoolmanagement-server-762p.onrender.com
