# Vite exposes only variables prefixed with VITE_
# Leave empty to use relative /api (dev: proxied to http://localhost:5000 in vite.config.ts)
# Set in production if the API is on another origin, e.g. VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
